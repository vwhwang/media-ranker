<h1>Users page</h1>
<table>
  <tr>
    <th>Username</th>
    <th>Votes</th>
    <th>Joined</th>
  </tr>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.username %></td>
      <td>0</td>
      <td><%=user.created_at.strftime('%B %d, %Y') %></td>
    </tr>
  <% end  %>
</table>